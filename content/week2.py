"""2주차 — 파이썬 시작, print(), 산술 연산, 스크립트 모드 (교재 1~4장)"""
from blocks import P, UL, CODE, TABLE, TIP, WARN, OUT, concept, problem, test

WEEK_NO = 2
TITLE = "파이썬 시작 · print()와 계산 · 스크립트 모드"
SUBTITLE = "교재 1장 (파이썬과 편집기) · 2장 (문장, 계산, 반복) · 3장 (스크립트 모드, 입력, 모듈)"

CONCEPTS = [
    concept("w2-idle", "대화형 모드와 스크립트 모드", [
        P("""
        파이썬을 쓰는 방법은 두 가지입니다. 수업에서는 **Thonny** 라는 편집기를 씁니다.
        (교재에는 IDLE 로 나오지만 화면 구성과 사용법은 거의 같습니다.)
        """),
        TABLE(["", "대화형 모드 (Shell)", "스크립트 모드 (편집기)"], [
            ["모습", "`>>>` 프롬프트가 있는 창", "파일을 편집하는 창"],
            ["실행", "한 줄 쓰면 바로 실행", "저장한 뒤 통째로 실행"],
            ["결과 보기", "값만 써도 결과가 보임", "`print()` 를 써야 보임"],
            ["쓰임", "짧게 시험해 볼 때", "프로그램을 만들 때"],
        ]),
        WARN("""
        이 차이가 헷갈림의 원인이 되는 경우가 많습니다.
        대화형 모드에서는 `10 + 20` 만 써도 `30` 이 보이지만,
        파일에 그렇게만 쓰면 **아무것도 출력되지 않습니다.**
        파일에서는 `print(10 + 20)` 이라고 써야 합니다.
        """),
        CODE("""
        10 + 20
        print(10 + 20)
        """, note="첫 줄은 계산만 하고 버립니다. 둘째 줄만 화면에 나옵니다."),
        P("파일로 저장할 때는 이름 끝에 **`.py`** 를 붙입니다. 예: `hello.py`"),
        TIP("""
        이 워크북의 편집기는 Thonny 와 비슷하게 만들어 두었습니다.
        실행하면 아래에 **Shell** 창처럼 결과가 나오고, 오른쪽 **Variables** 에
        변수에 무엇이 들어 있는지 보입니다. 수업 화면과 같은 감각으로 연습할 수 있습니다.
        """),
    ], summary="대화형 = 값만 써도 보임 / 스크립트(.py) = print() 필요"),

    concept("w2-print", "print() — 화면에 출력하기", [
        P("""
        파이썬에서 가장 먼저 쓰는 함수입니다. 괄호 안에 적은 값을 화면에 출력합니다.
        출력할 **문자열은 반드시 따옴표 안에** 있어야 하고, 여러 개를 출력할 땐 **쉼표**로 구분합니다.
        """),
        CODE("""
        print("Hello World!")
        print("안녕하세요")
        """),
        P("쉼표로 여러 값을 넘기면 파이썬이 **값 사이에 공백 한 칸**을 자동으로 넣어 줍니다."),
        CODE("""
        print("111을 4로 나누면?", 111 / 4)
        print("나이는", 25, "살입니다")
        """),
        TIP("""
        쉼표(`,`)와 더하기(`+`)는 다릅니다. 쉼표는 자동으로 공백을 넣고 자료형이 달라도 되지만,
        `+`는 공백을 넣지 않고 **문자열끼리만** 가능합니다. 3주차에서 자세히 봅니다.
        """),
    ], summary="print(값1, 값2, ...) — 쉼표로 구분하면 공백이 자동으로 들어간다"),

    concept("w2-escape", "특수 문자열 (이스케이프 문자)", [
        P("문자열 안에서 특별한 역할을 하는 기호들입니다. 모두 **역슬래시(`\\`)**로 시작합니다."),
        TABLE(["특수 문자열", "내용"], [
            ["\\n", "문자열의 줄을 바꿔 줍니다 (newline)"],
            ["\\t", "탭 형식의 들여쓰기를 삽입합니다 (tab)"],
            ["\\\\", "역슬래시(\\) 문자 자체를 표현합니다"],
            ["\\\"", "문자열 안에 큰따옴표를 넣습니다"],
            ["\\'", "문자열 안에 작은따옴표를 넣습니다"],
        ]),
        CODE("""
        print("Question: What goes clip?\\nAnswer: A one legged horse.")
        print("이름\\t나이\\t지역")
        print("윤인성\\t25\\t강서구")
        """, note="\\n 은 줄바꿈, \\t 는 탭입니다. 표처럼 정렬해서 출력할 때 유용합니다."),
        CODE("""
        print("Here is a speech mark: \\" and here is a slash: \\\\")
        print('I say "High", you say "Low".')
        """, note="따옴표 안에 같은 종류의 따옴표를 쓰려면 역슬래시를 붙이거나, 바깥을 다른 따옴표로 감쌉니다."),
        WARN("""
        `print("경로: C:\\new")` 처럼 쓰면 `\\n`이 줄바꿈으로 해석돼 의도와 다르게 나옵니다.
        역슬래시 자체를 쓰려면 `\\\\` 로 두 번 적으세요.
        """),
    ], summary="\\n 줄바꿈 · \\t 탭 · \\\\ 역슬래시 · \\\" 따옴표"),

    concept("w2-math", "산술 연산자", [
        P("파이썬은 계산기로도 훌륭합니다. 곱하기는 `*`, 나누기는 `/` 를 씁니다."),
        TABLE(["연산자", "의미", "예제", "결괏값"], [
            ["*", "곱하기", "2 * 3", "6"],
            ["/", "나누기 (일반)", "20 / 8", "2.5"],
            ["//", "나누기 (정수값만)", "20 // 8", "2"],
            ["%", "나머지", "20 % 8", "4"],
            ["+", "더하기", "2 + 3", "5"],
            ["-", "빼기", "7 - 3", "4"],
            ["**", "제곱", "2 ** 4", "16"],
        ]),
        CODE("""
        print("10 / 4 =", 10 / 4)
        print("11 / 4 =", 11 / 4)
        print("11 // 4 =", 11 // 4)
        print("11 % 4 =", 11 % 4)
        print("2 ** 10 =", 2 ** 10)
        """),
        TIP("""
        `/`는 나누어떨어져도 항상 실수(float)를 만듭니다. `10 / 5`의 결과는 `2`가 아니라 `2.0`입니다.
        정수가 필요하면 `//`를 쓰세요. 4주차 `range()`에서 이 차이가 실제 오류로 이어집니다.
        """),
        P("**연산자 우선순위**는 수학과 같습니다. 곱셈·나눗셈이 덧셈·뺄셈보다 먼저입니다."),
        CODE("""
        print(2 + 3 * 4)
        print((2 + 3) * 4)
        """, note="헷갈릴 것 같으면 확실할 때도 괄호로 감싸는 습관이 좋습니다."),
    ], summary="* 곱 · / 나누기(실수) · // 몫 · % 나머지 · ** 제곱"),

    concept("w2-while", "while 반복 맛보기", [
        P("""
        `while` 은 **어떤 조건이 참인 동안** 같은 일을 되풀이합니다.
        조건이 거짓이 되면 멈춥니다. 자세한 내용은 4주차에서 다시 봅니다.
        """),
        CODE("""
        lines = 0
        while lines < 5:
            print("나는 수업 시간에 프로그래밍을 하지 않겠습니다.")
            lines = lines + 1
        """, note="lines 가 0, 1, 2, 3, 4 일 때 참이라 다섯 번 출력됩니다."),
        P("같은 결과를 문자열 반복으로도 만들 수 있습니다."),
        CODE("""
        print("나는 수업 시간에 프로그래밍을 하지 않겠습니다.\\n" * 5)
        """, note="훨씬 짧습니다. 다만 회차마다 다른 일을 시킬 수는 없습니다."),
        P("`while` 조건에는 **비교 연산자**를 씁니다."),
        TABLE(["연산자", "의미", "연산자", "의미"], [
            ["==", "~와 같으면", "!=", "~와 같지 않다면"],
            [">", "~보다 크면", "<", "~보다 작으면"],
            [">=", "~보다 크거나 같으면", "<=", "~보다 작거나 같으면"],
        ]),
        WARN("""
        시작값에 따라 반복 횟수가 달라집니다.
        `lines = 0` 에서 `while lines < 50` 이면 **50번**이지만,
        `lines = 1` 에서 시작하면 **49번**입니다. 수업에서도 이 차이가 나왔습니다.
        """),
        WARN("""
        조건을 바꿔 주는 코드(`lines = lines + 1`)를 빠뜨리면 조건이 영원히 참이라
        **무한 반복**에 빠집니다. 이 워크북은 8초가 지나면 자동으로 멈춥니다.
        """),
    ], summary="while 조건: — 참인 동안 반복. 조건을 바꿔 주는 코드를 잊지 말 것"),

    concept("w2-comment", "주석과 변수 이름", [
        P("""
        `#` 뒤에 쓴 내용은 **주석**입니다. 컴퓨터는 무시하고, 사람이 읽으라고 남기는 메모입니다.
        """),
        CODE("""
        # 이 줄은 실행되지 않습니다
        print("안녕하세요")   # 줄 끝에도 붙일 수 있습니다
        # print("이 줄도 실행되지 않습니다")
        """, note="세 번째 줄처럼 코드를 잠시 꺼 둘 때도 주석을 씁니다."),
        P("코드를 깔끔하게 쓰려면 세 가지가 필요합니다."),
        UL(
            "**빈칸(들여쓰기)** — 코드를 묶으려면 **네 칸** 들여씁니다. 엔터를 치면 자동으로 들여써집니다.",
            "**주석** — `#` 로 설명을 남깁니다.",
            "**변수 이름** — 저장될 내용을 설명하는 이름이 가장 좋습니다.",
        ),
        WARN("""
        **예약어**는 변수 이름으로 쓸 수 없습니다.
        `and, as, assert, break, class, continue, def, del, elif, else, except, finally,
        for, from, global, if, import, in, is, lambda, not, or, pass, raise, return, try, while, with, yield`
        이름을 숫자로 시작하거나 특수문자(`&, !, @, $, *`)를 넣는 것도 안 됩니다.
        """),
        TIP("""
        `sum`, `list`, `str` 같은 이름은 예약어는 아니지만 파이썬이 이미 쓰고 있습니다.
        변수 이름으로 쓰면 원래 기능을 못 쓰게 되니 `sum_value` 처럼 바꿔 쓰는 편이 좋습니다.
        """),
    ], summary="# 주석 · 네 칸 들여쓰기 · 예약어는 변수 이름 불가"),

    concept("w2-input-random", "input()과 random 모듈 맛보기", [
        P("""
        `input()` 은 사용자가 키보드로 입력한 값을 받아 옵니다.
        괄호 안에 적은 문자열이 화면에 먼저 표시되고, 엔터를 누를 때까지 프로그램이 멈춥니다.
        """),
        CODE("""
        name = input("이름이 뭔가요?\\n")
        print("안녕", name)
        """, stdin=["김정미"], note="오른쪽 '입력값' 칸에 미리 넣어 둔 값이 순서대로 들어갑니다."),
        P("`input()` 에는 쓰임이 두 가지 있습니다."),
        UL(
            "**값을 받을 때** — `question = input(\"질문을 입력하세요\\\\n\")` 처럼 변수에 담습니다.",
            "**멈춰 세울 때** — `input(\"\\\\n마치려면 엔터를 누르세요.\")` 처럼 변수에 담지 않습니다.",
        ),
        P("""
        **모듈**은 특별한 코드로 만들어진 파이썬 파일입니다. `import` 로 불러오면
        그 안의 함수들을 쓸 수 있습니다. `random` 은 무작위 값을 만들어 주는 모듈입니다.
        """),
        CODE("""
        import random

        dice = random.randint(1, 6)
        print("주사위:", dice)
        """, note="randint(1, 6) 은 1 이상 6 이하의 정수를 무작위로 하나 고릅니다. 실행할 때마다 달라집니다.", nondeterministic=True),
        P("""
        괄호 안에 넣는 값을 **인수(argument)** 라고 합니다. 함수가 일하는 데 필요한 값입니다.
        """),
        WARN("`import random` 을 빠뜨리고 `random.randint(1, 6)` 을 쓰면 `NameError` 가 납니다."),
    ], summary="input() 입력받기 · import 모듈 · random.randint(a, b)"),

    concept("w2-error", "오류 메시지 읽는 법", [
        P("""
        코드를 잘못 쓰면 파이썬이 오류를 알려 줍니다. 오류는 실패가 아니라
        **어디가 잘못됐는지 알려주는 힌트**입니다. 자주 만나는 것부터 익혀 둡시다.
        """),
        TABLE(["오류", "언제 나는가", "예"], [
            ["SyntaxError", "문법이 잘못됨", "콜론(:)이나 괄호를 빠뜨림"],
            ["IndentationError", "들여쓰기가 잘못됨", "if 다음 줄을 들여쓰지 않음"],
            ["NameError", "없는 이름을 씀", "오타, 따옴표를 안 씌운 글자"],
            ["TypeError", "자료형이 안 맞음", "문자열 + 숫자"],
        ]),
        CODE("""
        print("안녕하세요" + 1)
        """, note="문자열과 숫자는 + 로 연결할 수 없습니다. TypeError 가 납니다."),
        CODE("""
        b = 5
        Print(b)
        """, note="Print 의 P 가 대문자라 NameError 가 납니다. 파이썬은 대소문자를 구분합니다."),
        TIP("""
        오류 메시지는 **맨 아래 줄부터** 읽으세요. 오류 종류와 이유가 거기 있습니다.
        그 위의 `line 2` 같은 표시가 몇 번째 줄이 문제인지 알려 줍니다.
        Thonny 는 오른쪽 Assistant 창에서 "콜론을 빠뜨리셨나요?" 처럼 더 친절히 알려 주기도 합니다.
        """),
    ], summary="SyntaxError 문법 · NameError 이름 · TypeError 자료형"),
]

PROBLEMS = [
    problem("w2-p1", "첫 출력", level=1, tags=["print"],
        prompt=[P("화면에 정확히 `Hello World!` 라고 출력하는 코드를 작성하세요.")],
        tests=[test()],
        hints=[
            "출력에는 print() 함수를 씁니다.",
            "출력할 글자는 따옴표로 감싸야 합니다. 큰따옴표든 작은따옴표든 괜찮습니다.",
            'print("Hello World!") 입니다. 느낌표까지 따옴표 안에 넣으세요.',
        ],
        solution='print("Hello World!")',
        explain=[
            P("`print()`는 괄호 안의 값을 화면에 출력합니다."),
            P("문자열은 따옴표로 감싸지 않으면 파이썬이 변수 이름으로 착각해 `NameError`가 납니다."),
        ]),

    problem("w2-p2", "두 줄로 출력하기", level=1, tags=["print", "이스케이프"],
        prompt=[
            P("`print()`를 **한 번만** 써서 아래처럼 두 줄로 출력하세요."),
            OUT("첫째 줄\n둘째 줄"),
        ],
        tests=[test()],
        hints=[
            "print()를 두 번 쓰지 않고 줄을 바꾸려면 특수 문자열이 필요합니다.",
            "줄바꿈을 뜻하는 특수 문자열은 \\n 입니다.",
            'print("첫째 줄\\n둘째 줄") 처럼 문자열 가운데에 \\n 을 넣습니다.',
        ],
        solution='print("첫째 줄\\n둘째 줄")',
        explain=[
            P("`\\n`은 문자 두 개가 아니라 **줄바꿈 하나**를 뜻하는 특수 문자열입니다."),
            P("문자열 안 어디에든 넣을 수 있어서, 여러 줄을 한 번의 `print()`로 출력할 수 있습니다."),
        ]),

    problem("w2-p3", "탭으로 표 만들기", level=2, tags=["print", "이스케이프"],
        prompt=[
            P("탭(`\\t`)을 사용해 아래와 같은 표를 출력하세요. `print()`는 세 번 쓰세요."),
            OUT("이름\t나이\t지역\n윤인성\t25\t강서구\n윤아린\t24\t강서구"),
        ],
        tests=[test()],
        hints=[
            "탭은 \\t 입니다. 각 항목 사이에 넣으세요.",
            '첫 줄은 print("이름\\t나이\\t지역") 입니다.',
            "나머지 두 줄도 같은 방식으로, 값만 바꿔 쓰면 됩니다.",
        ],
        solution='print("이름\\t나이\\t지역")\nprint("윤인성\\t25\\t강서구")\nprint("윤아린\\t24\\t강서구")',
        explain=[
            P("`\\t`는 다음 탭 위치까지 커서를 옮깁니다. 항목 길이가 비슷하면 표처럼 정렬돼 보입니다."),
            TIP("글자 수가 크게 다르면 정렬이 어긋납니다. 정확한 정렬은 `format()`이나 f-문자열을 씁니다."),
        ]),

    problem("w2-p4", "나눗셈 세 가지", level=2, tags=["연산자"],
        prompt=[
            P("`17`을 `5`로 나눈 결과를 세 가지 방식으로 각각 한 줄씩 출력하세요."),
            OUT("3.4\n3\n2"),
            P("순서대로 일반 나누기, 몫, 나머지입니다."),
        ],
        tests=[test()],
        hints=[
            "일반 나누기는 /, 몫은 //, 나머지는 % 입니다.",
            "print(17 / 5) 처럼 계산식을 그대로 print() 안에 넣을 수 있습니다.",
            "print(17 / 5), print(17 // 5), print(17 % 5) 세 줄입니다.",
        ],
        solution="print(17 / 5)\nprint(17 // 5)\nprint(17 % 5)",
        explain=[
            P("`/`는 항상 실수를 만들기 때문에 `3.4`가 나옵니다."),
            P("`//`는 소수점 아래를 버리고 정수 부분만 남겨 `3`이 됩니다."),
            P("`%`는 나머지라서 `17 = 5 × 3 + 2`의 `2`가 됩니다."),
            TIP("`%`는 짝수/홀수 판별에 자주 씁니다. `n % 2 == 0`이면 짝수입니다. 5주차에서 다시 만납니다."),
        ]),

    problem("w2-p5", "실행 결과 예측 — 연산자 우선순위", level=2, tags=["연산자"], ptype="predict",
        prompt=[P("아래 코드의 실행 결과를 예측해서 입력하세요.")],
        code="""
        print(2 + 3 * 4)
        print((2 + 3) * 4)
        print(10 - 2 ** 3)
        """,
        hints=[
            "곱셈과 나눗셈이 덧셈과 뺄셈보다 먼저 계산됩니다.",
            "괄호가 있으면 괄호 안이 가장 먼저입니다.",
            "제곱(**)은 곱셈보다도 우선순위가 높습니다. 10 - 2**3 = 10 - 8 입니다.",
        ],
        explain=[
            P("우선순위는 `**` → `* / // %` → `+ -` 순입니다."),
            P("첫 줄은 `3*4=12`를 먼저 계산해 `14`, 둘째 줄은 괄호가 먼저라 `5*4=20`입니다."),
            P("셋째 줄은 `2**3=8`이 먼저라 `10-8=2`가 됩니다."),
        ]),

    problem("w2-p6", "대소문자 오류 찾기", level=1, tags=["오류"], ptype="choice",
        prompt=[P("아래 코드 중 **오류가 발생하는 것**을 고르세요.")],
        options=[
            {"text": "①", "code": 'b = 5\nprint(b)'},
            {"text": "②", "code": 'b = 5\nPrint(b)'},
            {"text": "③", "code": 'B = 5\nprint(B)'},
            {"text": "④", "code": 'b = 5\nprint(b + 1)'},
        ],
        answer=1,
        hints=[
            "파이썬은 대문자와 소문자를 다른 글자로 봅니다.",
            "함수 이름도 정확히 써야 합니다.",
            "출력 함수의 이름은 print 입니다. 첫 글자가 대문자면 어떻게 될까요?",
        ],
        explain=[
            P("②가 `NameError: name 'Print' is not defined` 를 냅니다."),
            P("파이썬은 대소문자를 구분하므로 `Print`와 `print`는 완전히 다른 이름입니다."),
            P("③처럼 변수 이름을 대문자로 만드는 것은 괜찮습니다. 만들 때와 쓸 때가 같기만 하면 됩니다."),
            TIP("초보자가 가장 자주 겪는 오류입니다. 오류 메시지에 나온 이름의 철자를 그대로 확인해 보세요."),
        ]),

    problem("w2-p7", "while 로 다섯 번 출력하기", level=2, tags=["while"],
        prompt=[
            P("`while` 반복문으로 `안녕하세요` 를 **다섯 번** 출력하세요."),
            OUT("안녕하세요\n안녕하세요\n안녕하세요\n안녕하세요\n안녕하세요"),
            P("반복 횟수를 세는 변수는 `0` 부터 시작하세요."),
        ],
        tests=[test()],
        hints=[
            "먼저 세는 변수를 만듭니다. lines = 0",
            "while lines < 5: 로 조건을 걸고 다음 줄은 네 칸 들여씁니다.",
            "반복문 안에서 print 를 하고 lines = lines + 1 로 값을 키워야 멈춥니다.",
        ],
        solution='lines = 0\nwhile lines < 5:\n    print("안녕하세요")\n    lines = lines + 1',
        explain=[
            P("`lines`가 `0, 1, 2, 3, 4`일 때 조건이 참이라 다섯 번 실행됩니다. `5`가 되면 멈춥니다."),
            WARN("`lines = 1`로 시작하면 네 번만 출력됩니다. 수업에서도 `lines = 1`, `while lines < 50`이라 50번이 아닌 49번이 나왔습니다."),
            TIP("`lines = lines + 1`을 빠뜨리면 조건이 영원히 참이라 무한 반복에 빠집니다."),
        ]),

    problem("w2-p8", "주석은 실행되지 않는다", level=1, tags=["주석"], ptype="predict",
        prompt=[P("아래 코드의 실행 결과를 예측하세요.")],
        code="""
        # print("첫째")
        print("둘째")   # print("셋째")
        print("넷째")
        """,
        hints=[
            "# 뒤에 오는 내용은 컴퓨터가 무시합니다.",
            "줄 맨 앞에 # 이 있으면 그 줄 전체가 주석입니다.",
            "줄 중간에 # 이 있으면 그 뒤부터 끝까지가 주석입니다.",
        ],
        explain=[
            P("첫 줄은 전체가 주석이라 실행되지 않습니다."),
            P("둘째 줄은 `print(\"둘째\")` 까지만 실행되고 `#` 뒤는 무시됩니다."),
            P("셋째 줄은 그대로 실행됩니다."),
            TIP("코드를 지우지 않고 잠시 꺼 둘 때 주석이 유용합니다. 수업에서도 for 문을 주석 처리하고 while 로 바꿔 보셨습니다."),
        ]),

    problem("w2-p9", "원의 넓이와 둘레", level=3, tags=["연산자", "print"],
        prompt=[
            P("반지름이 `10`인 원의 **둘레**와 **넓이**를 아래 형식으로 출력하세요. 원주율은 `3.14159265`를 쓰세요."),
            OUT("원의 둘레 = 62.831853\n원의 넓이 = 314.159265"),
            P("둘레는 `2 × 파이 × r`, 넓이는 `파이 × r × r` 입니다."),
        ],
        tests=[test()],
        hints=[
            "print()에 쉼표를 쓰면 문자열과 계산 결과를 함께 출력할 수 있습니다.",
            'print("원의 둘레 =", 2 * 3.14159265 * 10) 형태입니다.',
            "넓이도 같은 방식으로 3.14159265 * 10 * 10 을 계산합니다.",
        ],
        solution='print("원의 둘레 =", 2 * 3.14159265 * 10)\nprint("원의 넓이 =", 3.14159265 * 10 * 10)',
        explain=[
            P("쉼표로 구분하면 문자열과 숫자를 함께 출력할 수 있고, 사이에 공백이 자동으로 들어갑니다."),
            TIP("""
            소수 계산에서는 가끔 `28.259999999999998` 처럼 끝자리가 이상하게 나옵니다.
            컴퓨터가 소수를 2진수로 저장하면서 생기는 아주 작은 오차입니다.
            이 워크북은 채점할 때 이런 오차를 무시하므로 계산식만 맞으면 정답입니다.
            """),
            TIP("같은 값(3.14159265)을 여러 번 쓰고 있죠? 3주차에서 배울 **변수**를 쓰면 한 번만 적으면 됩니다."),
        ]),
]
