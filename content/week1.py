"""1주차 — 파이썬 첫걸음, print()와 문자열, 산술 연산 (교재 1~2장)"""
from blocks import P, UL, CODE, TABLE, TIP, WARN, OUT, concept, problem, test

TITLE = "파이썬 시작 · print()와 문자열 · 산술 연산"
SUBTITLE = "교재 1장 (파이썬과 IDLE) + 2장 (문장, 계산, 반복문 맛보기)"

CONCEPTS = [
    concept("w1-print", "print() — 화면에 출력하기", [
        P("""
        파이썬에서 가장 먼저 쓰는 함수입니다. 괄호 안에 적은 값을 화면에 출력합니다.
        출력할 **문자열은 반드시 따옴표 안에** 있어야 하고, 여러 개를 출력할 땐 **쉼표**로 구분합니다.
        """),
        CODE("""
        print("Hello World!")
        print("안녕하세요")
        """),
        P("쉼표로 여러 값을 넘기면 파이썬이 **값 사이에 공백 한 칸**을 자동으로 넣어 줍니다."),
        CODE("""
        print("111을 4로 나누면?", 111 / 4)
        print("나이는", 25, "살입니다")
        """),
        TIP("""
        쉼표(`,`)와 더하기(`+`)는 다릅니다. 쉼표는 자동으로 공백을 넣고 자료형이 달라도 되지만,
        `+`는 공백을 넣지 않고 **문자열끼리만** 가능합니다. 이건 2주차에서 자세히 봅니다.
        """),
    ], summary="print(값1, 값2, ...) — 쉼표로 구분하면 공백이 자동으로 들어간다"),

    concept("w1-escape", "특수 문자열 (이스케이프 문자)", [
        P("문자열 안에서 특별한 역할을 하는 기호들입니다. 모두 **역슬래시(`\\`)**로 시작합니다."),
        TABLE(["특수 문자열", "내용"], [
            ["\\n", "문자열의 줄을 바꿔 줍니다 (newline)"],
            ["\\t", "탭 형식의 들여쓰기를 삽입합니다 (tab)"],
            ["\\\\", "역슬래시(\\) 문자 자체를 표현합니다"],
            ["\\\"", "문자열 안에 큰따옴표를 넣습니다"],
            ["\\'", "문자열 안에 작은따옴표를 넣습니다"],
        ]),
        CODE("""
        print("Question: What goes clip?\\nAnswer: A one legged horse.")
        print("이름\\t나이\\t지역")
        print("윤인성\\t25\\t강서구")
        """, note="\\n 은 줄바꿈, \\t 는 탭입니다. 표처럼 정렬해서 출력할 때 유용합니다."),
        CODE("""
        print("Here is a speech mark: \\" and here is a slash: \\\\")
        print('I say "High", you say "Low".')
        """, note="따옴표 안에 같은 종류의 따옴표를 쓰려면 역슬래시를 붙이거나, 바깥을 다른 따옴표로 감쌉니다."),
        WARN("""
        `print("경로: C:\\new")` 처럼 쓰면 `\\n`이 줄바꿈으로 해석돼 의도와 다르게 나옵니다.
        역슬래시 자체를 쓰려면 `\\\\` 로 두 번 적으세요.
        """),
    ], summary="\\n 줄바꿈 · \\t 탭 · \\\\ 역슬래시 · \\\" 따옴표"),

    concept("w1-math", "산술 연산자", [
        P("파이썬은 계산기로도 훌륭합니다. 곱하기는 `*`, 나누기는 `/` 를 씁니다."),
        TABLE(["연산자", "의미", "예제", "결괏값"], [
            ["*", "곱하기", "2 * 3", "6"],
            ["/", "나누기 (일반)", "20 / 8", "2.5"],
            ["//", "나누기 (정수값만)", "20 // 8", "2"],
            ["%", "나머지", "20 % 8", "4"],
            ["+", "더하기", "2 + 3", "5"],
            ["-", "빼기", "7 - 3", "4"],
            ["**", "제곱", "2 ** 4", "16"],
        ]),
        CODE("""
        print("10 / 4 =", 10 / 4)
        print("11 / 4 =", 11 / 4)
        print("11 // 4 =", 11 // 4)
        print("11 % 4 =", 11 % 4)
        print("2 ** 10 =", 2 ** 10)
        """),
        TIP("""
        `/`는 나누어떨어져도 항상 실수(float)를 만듭니다. `10 / 5`의 결과는 `2`가 아니라 `2.0`입니다.
        정수가 필요하면 `//`를 쓰세요. 5주차 `range()`에서 이 차이가 실제 오류로 이어집니다.
        """),
        P("**연산자 우선순위**는 수학과 같습니다. 곱셈·나눗셈이 덧셈·뺄셈보다 먼저입니다."),
        CODE("""
        print(2 + 3 * 4)
        print((2 + 3) * 4)
        """, note="헷갈릴 것 같으면 확실할 때도 괄호로 감싸는 습관이 좋습니다."),
    ], summary="* 곱 · / 나누기(실수) · // 몫 · % 나머지 · ** 제곱"),

    concept("w1-error", "오류 메시지 읽는 법", [
        P("""
        코드를 잘못 쓰면 파이썬이 오류를 알려 줍니다. 오류는 실패가 아니라 **어디가 잘못됐는지 알려주는 힌트**입니다.
        가장 자주 만나는 두 가지를 먼저 익혀 둡시다.
        """),
        UL(
            "`SyntaxError` — 문법이 잘못됐습니다. 괄호나 따옴표를 안 닫은 경우가 대부분입니다.",
            "`TypeError` — 자료형이 안 맞습니다. 문자열과 숫자를 `+`로 붙이려 할 때 나옵니다.",
        ),
        CODE("""
        print("안녕하세요" + 1)
        """, note="문자열과 숫자는 + 로 연결할 수 없습니다. TypeError 가 납니다.", live=True),
        TIP("""
        오류 메시지는 **맨 아래 줄부터** 읽으세요. 오류 종류와 이유가 거기 있습니다.
        그 위의 `line 2` 같은 표시가 몇 번째 줄이 문제인지 알려 줍니다.
        """),
    ], summary="SyntaxError = 문법(괄호·따옴표), TypeError = 자료형 불일치"),
]

PROBLEMS = [
    problem("w1-p1", "첫 출력", level=1, tags=["print"],
        prompt=[P("화면에 정확히 `Hello World!` 라고 출력하는 코드를 작성하세요.")],
        starter="",
        tests=[test()],
        hints=[
            "출력에는 print() 함수를 씁니다.",
            "출력할 글자는 따옴표로 감싸야 합니다. 큰따옴표든 작은따옴표든 괜찮습니다.",
            'print("Hello World!") 입니다. 느낌표까지 따옴표 안에 넣으세요.',
        ],
        solution='print("Hello World!")',
        explain=[
            P("`print()`는 괄호 안의 값을 화면에 출력합니다."),
            P("문자열은 따옴표로 감싸지 않으면 파이썬이 변수 이름으로 착각해 `NameError`가 납니다."),
        ]),

    problem("w1-p2", "두 줄로 출력하기", level=1, tags=["print", "이스케이프"],
        prompt=[
            P("`print()`를 **한 번만** 써서 아래처럼 두 줄로 출력하세요."),
            OUT("첫째 줄\n둘째 줄"),
        ],
        tests=[test()],
        hints=[
            "print()를 두 번 쓰지 않고 줄을 바꾸려면 특수 문자열이 필요합니다.",
            "줄바꿈을 뜻하는 특수 문자열은 \\n 입니다.",
            'print("첫째 줄\\n둘째 줄") 처럼 문자열 가운데에 \\n 을 넣습니다.',
        ],
        solution='print("첫째 줄\\n둘째 줄")',
        explain=[
            P("`\\n`은 문자 두 개가 아니라 **줄바꿈 하나**를 뜻하는 특수 문자열입니다."),
            P("문자열 안 어디에든 넣을 수 있어서, 여러 줄을 한 번의 `print()`로 출력할 수 있습니다."),
        ]),

    problem("w1-p3", "탭으로 표 만들기", level=2, tags=["print", "이스케이프"],
        prompt=[
            P("탭(`\\t`)을 사용해 아래와 같은 표를 출력하세요. `print()`는 세 번 쓰세요."),
            OUT("이름\t나이\t지역\n윤인성\t25\t강서구\n윤아린\t24\t강서구"),
        ],
        tests=[test()],
        hints=[
            "탭은 \\t 입니다. 각 항목 사이에 넣으세요.",
            '첫 줄은 print("이름\\t나이\\t지역") 입니다.',
            "나머지 두 줄도 같은 방식으로, 값만 바꿔 쓰면 됩니다.",
        ],
        solution='print("이름\\t나이\\t지역")\nprint("윤인성\\t25\\t강서구")\nprint("윤아린\\t24\\t강서구")',
        explain=[
            P("`\\t`는 다음 탭 위치까지 커서를 옮깁니다. 항목 길이가 비슷하면 표처럼 정렬돼 보입니다."),
            TIP("글자 수가 크게 다르면 정렬이 어긋납니다. 정확한 정렬은 나중에 배울 `format()`이나 f-문자열을 씁니다."),
        ]),

    problem("w1-p4", "나눗셈 세 가지", level=2, tags=["연산자"],
        prompt=[
            P("`17`을 `5`로 나눈 결과를 세 가지 방식으로 각각 한 줄씩 출력하세요."),
            OUT("3.4\n3\n2"),
            P("순서대로 일반 나누기, 몫, 나머지입니다."),
        ],
        tests=[test()],
        hints=[
            "일반 나누기는 /, 몫은 //, 나머지는 % 입니다.",
            "print(17 / 5) 처럼 계산식을 그대로 print() 안에 넣을 수 있습니다.",
            "print(17 / 5), print(17 // 5), print(17 % 5) 세 줄입니다.",
        ],
        solution="print(17 / 5)\nprint(17 // 5)\nprint(17 % 5)",
        explain=[
            P("`/`는 항상 실수를 만들기 때문에 `3.4`가 나옵니다."),
            P("`//`는 소수점 아래를 버리고 정수 부분만 남겨 `3`이 됩니다."),
            P("`%`는 나머지라서 `17 = 5 × 3 + 2`의 `2`가 됩니다."),
            TIP("`%`는 짝수/홀수 판별에 자주 씁니다. `n % 2 == 0`이면 짝수입니다. 6주차에서 다시 만납니다."),
        ]),

    problem("w1-p5", "실행 결과 예측 — 연산자 우선순위", level=2, tags=["연산자"], ptype="predict",
        prompt=[P("아래 코드의 실행 결과를 예측해서 입력하세요.")],
        code="""
        print(2 + 3 * 4)
        print((2 + 3) * 4)
        print(10 - 2 ** 3)
        """,
        hints=[
            "곱셈과 나눗셈이 덧셈과 뺄셈보다 먼저 계산됩니다.",
            "괄호가 있으면 괄호 안이 가장 먼저입니다.",
            "제곱(**)은 곱셈보다도 우선순위가 높습니다. 10 - 2**3 = 10 - 8 입니다.",
        ],
        explain=[
            P("우선순위는 `**` → `* / // %` → `+ -` 순입니다."),
            P("첫 줄은 `3*4=12`를 먼저 계산해 `14`, 둘째 줄은 괄호가 먼저라 `5*4=20`입니다."),
            P("셋째 줄은 `2**3=8`이 먼저라 `10-8=2`가 됩니다."),
        ]),

    problem("w1-p6", "원의 넓이와 둘레", level=3, tags=["연산자", "print"],
        prompt=[
            P("반지름이 `5`인 원의 **둘레**와 **넓이**를 아래 형식으로 출력하세요. 원주율은 `3.14159265`를 쓰세요."),
            OUT("원의 둘레 = 31.4159265\n원의 넓이 = 78.53981625"),
            P("둘레는 `2 × 파이 × r`, 넓이는 `파이 × r × r` 입니다."),
        ],
        tests=[test()],
        hints=[
            "print()에 쉼표를 쓰면 문자열과 계산 결과를 함께 출력할 수 있습니다.",
            'print("원의 둘레 =", 2 * 3.14159265 * 5) 형태입니다.',
            "넓이도 같은 방식으로 3.14159265 * 5 * 5 를 계산합니다.",
        ],
        solution='print("원의 둘레 =", 2 * 3.14159265 * 5)\nprint("원의 넓이 =", 3.14159265 * 5 * 5)',
        explain=[
            P("쉼표로 구분하면 문자열과 숫자를 함께 출력할 수 있고, 사이에 공백이 자동으로 들어갑니다."),
            TIP("""
            소수 계산에서는 가끔 `28.259999999999998` 처럼 끝자리가 이상하게 나옵니다.
            컴퓨터가 소수를 2진수로 저장하면서 생기는 아주 작은 오차로, 파이썬만의 문제가 아닙니다.
            이 학습 앱은 채점할 때 이런 오차를 무시하므로, 계산식만 맞으면 정답으로 처리됩니다.
            """),
            TIP("같은 값(3.14159265)을 여러 번 쓰고 있죠? 2주차에서 배울 **변수**를 쓰면 `pi = 3.14159265` 한 번만 적으면 됩니다."),
        ]),
]
