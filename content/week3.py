"""3주차 — 변수와 입력, 자료형과 문자열, 숫자 (혼공파 「변수와 입력」·「자료형과 문자열」·「숫자」)"""
from blocks import P, UL, CODE, TABLE, TIP, WARN, OUT, concept, problem, test

WEEK_NO = 3
TITLE = "변수와 입력 · 자료형과 문자열 · 숫자"
SUBTITLE = "혼공파 「변수와 입력」 · 「자료형과 문자열」 · 「숫자」"

CONCEPTS = [
    concept("w3-var", "변수 — 값에 이름 붙이기", [
        P("""
        변수는 값을 저장할 때 쓰는 **이름표**입니다. 숫자뿐 아니라 모든 자료형을 저장할 수 있습니다.
        `=` 기호는 '같다'가 아니라 **오른쪽 값을 왼쪽 이름에 넣어라**는 뜻입니다.
        """),
        UL(
            "**선언** — 변수를 만드는 것",
            "**할당** — 변수에 값을 넣는 것 (`abc = 5`)",
            "**참조** — 변수에서 값을 꺼내 쓰는 것 (`print(abc)`)",
        ),
        CODE("""
        # 변수 선언과 할당
        pi = 3.14159265
        r = 10

        # 변수 참조
        print("원주율 =", pi)
        print("반지름 =", r)
        print("원의 둘레 =", 2 * pi * r)
        print("원의 넓이 =", pi * r * r)
        """, note="값에 이름을 붙여 두면 같은 값을 여러 번 쓸 때 편하고, 나중에 고치기도 쉽습니다."),
        P("변수에 담긴 것이 숫자면 숫자 연산을 모두 할 수 있습니다."),
        CODE("""
        pi = 3.14159265

        print(pi + 2)
        print(pi * 2)
        print(pi / 2)
        print(pi * pi)
        """),
        WARN("""
        숫자가 담긴 변수와 문자열은 `+` 로 이을 수 없습니다.
        `pi + "문자열"` 은 `TypeError` 입니다.
        """),
        TIP("좋은 변수 이름은 **저장될 내용을 설명하는 이름**입니다. `a`보다 `student_count`가 낫습니다."),
    ], summary="변수 = 값에 붙이는 이름표. 선언 → 할당 → 참조"),

    concept("w3-compound", "복합 대입 연산자", [
        P("기본 연산자와 `=`를 함께 써서, 변수의 현재 값을 이용해 새 값을 넣습니다."),
        TABLE(["연산자", "설명", "같은 뜻"], [
            ["+=", "숫자 덧셈 후 대입", "a = a + 10"],
            ["-=", "숫자 뺄셈 후 대입", "a = a - 10"],
            ["*=", "숫자 곱셈 후 대입", "a = a * 10"],
            ["/=", "숫자 나눗셈 후 대입", "a = a / 10"],
            ["%=", "나머지를 구한 후 대입", "a = a % 10"],
            ["**=", "제곱 후 대입", "a = a ** 10"],
        ]),
        CODE("""
        number = 100
        number += 10
        number += 20
        number += 30
        print("number:", number)
        """),
        P("문자열에도 `+=`(연결)와 `*=`(반복)를 쓸 수 있습니다."),
        CODE("""
        string = "안녕하세요"
        string += "!"
        string += "!"
        print("string:", string)
        """),
        TIP("`+=`는 4주차 반복문에서 합계를 구할 때 거의 항상 등장합니다. 지금 익혀 두면 편합니다."),
    ], summary="a += 10 은 a = a + 10 의 줄임말"),

    concept("w3-input", "input() — 사용자 입력 받기", [
        P("""
        `input()`은 사용자가 키보드로 입력한 값을 받아 옵니다.
        괄호 안에 적은 문자열은 **프롬프트**로 화면에 먼저 표시됩니다.
        엔터를 누를 때까지 프로그램은 **블록**(잠시 멈춤) 상태가 됩니다.
        """),
        CODE("""
        string = input("인사말을 입력하세요> ")
        print(string)
        """, stdin=["안녕하세요"], note="입력받은 값을 변수에 담아 두면 여러 번 쓸 수 있습니다."),
        WARN("""
        **가장 중요한 규칙:** `input()`의 결과는 사용자가 무엇을 입력하든 **무조건 문자열(str)** 입니다.
        `52273`을 입력해도 숫자가 아니라 문자열 `"52273"`이 됩니다.
        """),
        CODE("""
        string = input("입력> ")
        print("자료:", string)
        print("자료형:", type(string))
        """, stdin=["52273"], note="type() 함수로 자료형을 확인할 수 있습니다."),
        CODE("""
        string = input("입력> ")
        print("자료:", string)
        print("자료형:", type(string))
        """, stdin=["True"], note="True 를 입력해도 불이 아니라 문자열입니다."),
        CODE("""
        string = input("입력> ")
        print("입력 + 100:", string + 100)
        """, stdin=["300"], note="문자열 + 숫자라서 TypeError 가 납니다. 이걸 해결하는 게 다음 개념입니다."),
    ], summary="input()의 결과는 언제나 문자열(str)"),

    concept("w3-cast", "자료형 변환 — int(), float(), str()", [
        P("""
        입력받은 문자열을 계산에 쓰려면 숫자로 바꿔야 합니다. 이것을 **캐스트(cast)** 라고 합니다.
        """),
        UL(
            "`int(\"300\")` → `300` — 문자열을 정수로",
            "`float(\"52.273\")` → `52.273` — 문자열을 실수로",
            "`str(52)` → `\"52\"` — 숫자를 문자열로",
        ),
        CODE("""
        string_a = input("입력A> ")
        int_a = int(string_a)

        string_b = input("입력B> ")
        int_b = int(string_b)

        print("문자열 자료:", string_a + string_b)
        print("숫자 자료:", int_a + int_b)
        """, stdin=["273", "52"], note="같은 입력이라도 문자열이면 이어 붙고(27352), 숫자면 더해집니다(325)."),
        P("`input()`과 `int()`를 한 줄로 합쳐 쓰는 게 더 흔한 방식입니다."),
        CODE("""
        input_a = float(input("첫 번째 숫자> "))
        input_b = float(input("두 번째 숫자> "))

        print("덧셈 결과:", input_a + input_b)
        print("나눗셈 결과:", input_a / input_b)
        """, stdin=["273", "52"], note="괄호 안쪽(input)이 먼저 실행되고, 그 결과가 float() 로 들어갑니다."),
        P("반대로 숫자를 문자열로 바꿀 때는 `str()` 을 씁니다."),
        CODE("""
        output_a = str(52)
        output_b = str(52.273)
        print(type(output_a), output_a)
        print(type(output_b), output_b)
        """),
        WARN("""
        **ValueError** — 숫자로 바꿀 수 없는 것을 바꾸려 하면 납니다.
        `int("안녕하세요")` 는 물론이고, **`int("52.273")` 도 오류**입니다.
        소수점이 있는 문자열은 `int()`가 아니라 `float()`로 변환해야 합니다.
        """),
        CODE("""
        print(int("52.273"))
        """, note="ValueError: invalid literal for int() with base 10: '52.273'"),
    ], summary="int()/float() 문자열→숫자, str() 숫자→문자열. int(\"52.273\")은 ValueError"),

    concept("w3-type", "자료형 — 자료의 형식", [
        P("""
        프로그램이 처리할 수 있는 모든 것을 **자료(data)** 라 하고, 자료를 기능과 역할에 따라 구분한 것이
        **자료형(data type)** 입니다. `type()` 함수로 확인할 수 있습니다.
        """),
        TABLE(["자료형", "이름", "예"], [
            ["str", "문자열 (string)", '"안녕하세요", "52"'],
            ["int", "정수 (integer)", "0, 273, -52"],
            ["float", "실수 (부동소수점)", "0.0, 52.273, -1.2"],
            ["bool", "불 (boolean)", "True, False"],
        ]),
        CODE("""
        print(type("안녕하세요"))
        print(type(273))
        print(type(52.273))
        print(type(True))
        """),
        TIP("`int`와 `float`는 둘 다 숫자지만 다른 자료형입니다. `52`와 `52.0`은 값은 같아도 형식이 다릅니다."),
    ], summary="type()으로 확인. str · int · float · bool"),

    concept("w3-string", "문자열 만들기", [
        P("문자열은 큰따옴표(`\"`)나 작은따옴표(`'`)로 감싸 만듭니다. 둘 다 똑같이 동작합니다."),
        CODE("""
        print("큰따옴표로 만든 문자열")
        print('작은따옴표로 만든 문자열')
        """),
        P("문자열 **안에** 따옴표를 넣어야 할 때는 바깥을 다른 종류로 감싸면 간단합니다."),
        CODE("""
        print('"안녕하세요"라고 말했습니다')
        print("'안녕하세요'라고 말했습니다")
        print("역슬래시를 써도 됩니다: \\"안녕\\"")
        """),
        P("여러 줄 문자열은 따옴표를 **세 번** 반복해 만듭니다."),
        CODE('''
        print("""동해물과 백두산이 마르고 닳도록
        하느님이 보우하사 우리나라 만세""")
        ''', note="\\n 을 일일이 넣지 않아도 줄바꿈이 그대로 유지됩니다."),
        WARN("""
        여러 줄 문자열은 시작하는 `\"\"\"` 바로 뒤와 끝나는 `\"\"\"` 앞에서 **의도치 않은 줄바꿈**이 생깁니다.
        줄 끝에 `\\` 를 붙이면 '여기서 줄을 바꾸지 않겠다'는 뜻이 되어 이를 막을 수 있습니다.
        """),
    ], summary='따옴표 / 작은따옴표 / """여러 줄"""'),

    concept("w3-strop", "문자열 연산자", [
        P("자료마다 쓸 수 있는 연산자가 정해져 있습니다. 문자열에는 네 가지가 있습니다."),
        UL(
            "**연결 연산자 `+`** — 두 문자열을 이어 새 문자열을 만듭니다",
            "**반복 연산자 `*`** — 문자열을 숫자만큼 반복합니다",
            "**선택 연산자 `[]`** — 문자 하나를 고릅니다 (인덱싱)",
            "**범위 선택 연산자 `[:]`** — 일부를 잘라 냅니다 (슬라이싱)",
        ),
        CODE("""
        print("안녕" + "하세요")
        print("안녕하세요" * 3)
        print(3 * "안녕하세요")
        """),
        CODE("""
        print("안녕하세요" + 1)
        """, note="문자열과 숫자는 + 로 연결할 수 없습니다. TypeError 입니다."),
        WARN("`+`는 문자열끼리만, 숫자는 숫자끼리만 가능합니다. 숫자를 붙이려면 `str()`로 바꾸세요."),
    ], summary="+ 연결 · * 반복 · [] 선택 · [:] 범위"),

    concept("w3-index", "인덱싱 — 문자 하나 고르기", [
        P("""
        파이썬은 **제로 인덱스**를 씁니다. 첫 글자가 `0`번입니다.
        대괄호 안에 위치를 적으면 그 자리의 문자 하나를 꺼냅니다.
        """),
        TABLE(["문자", "안", "녕", "하", "세", "요"], [
            ["앞에서", "[0]", "[1]", "[2]", "[3]", "[4]"],
            ["뒤에서", "[-5]", "[-4]", "[-3]", "[-2]", "[-1]"],
        ]),
        CODE("""
        print("안녕하세요"[0])
        print("안녕하세요"[2])
        print("안녕하세요"[-1])
        print("안녕하세요"[-2])
        """, note="음수를 쓰면 뒤에서부터 셉니다. [-1] 이 마지막 글자입니다."),
        WARN("""
        **IndexError** — 문자 개수를 넘는 위치를 고르면 납니다.
        `\"안녕하세요\"`는 5글자이므로 쓸 수 있는 인덱스는 `0~4` 뿐입니다.
        """),
        CODE("""
        print("안녕하세요"[10])
        """, note="IndexError: string index out of range"),
    ], summary="[0]이 첫 글자, [-1]이 마지막 글자"),

    concept("w3-slice", "슬라이싱 — 범위 잘라내기", [
        P("""
        `[시작:끝]` 형태로 문자열의 일부를 잘라 냅니다.
        **시작은 포함, 끝은 포함하지 않습니다.** 이게 파이썬 슬라이싱의 핵심 규칙입니다.
        """),
        CODE("""
        print("안녕하세요"[1:4])
        print("안녕하세요"[0:2])
        print("안녕하세요"[2:4])
        """, note="[1:4] 는 1, 2, 3번 글자입니다. 4번(요)은 포함되지 않습니다."),
        P("둘 중 하나를 생략할 수도 있습니다."),
        UL(
            "`[1:]` — 1번부터 **끝까지**",
            "`[:3]` — 0번부터 **3번 앞까지**",
            "`[:]` — 전체",
        ),
        CODE("""
        print("안녕하세요"[1:])
        print("안녕하세요"[:3])
        """),
        TIP("""
        슬라이싱은 인덱싱과 달리 **범위를 넘어가도 오류가 나지 않습니다.**
        `\"안녕하세요\"[2:100]` 은 오류 없이 `\"하세요\"` 를 돌려줍니다.
        """),
        P("**`len()`** 은 문자열의 문자 개수를 세어 줍니다."),
        CODE("""
        print(len("안녕하세요"))
        print(len("Hello"))
        print(len(""))
        """),
    ], summary="[시작:끝] — 시작은 포함, 끝은 미포함 · len()은 길이"),

    concept("w3-number", "숫자 자료형과 연산자 우선순위", [
        P("파이썬은 숫자를 소수점이 없는 것과 있는 것으로 나눕니다."),
        UL(
            "**정수형(int)** — 소수점이 없는 숫자. `0, 1, 273, -52`",
            "**실수형(float)** — 소수점이 있는 숫자. `0.0, 52.273, -1.2` (부동 소수점)",
        ),
        CODE("""
        print("5 + 7 =", 5 + 7)
        print("5 - 7 =", 5 - 7)
        print("5 * 7 =", 5 * 7)
        print("5 / 7 =", 5 / 7)
        print("5 // 7 =", 5 // 7)
        print("5 % 7 =", 5 % 7)
        print("5 ** 7 =", 5 ** 7)
        """),
        P("""
        우선순위는 **`**` → `* / // %` → `+ -`** 순입니다.
        헷갈릴 때는 괄호로 감싸는 것이 가장 안전합니다.
        """),
        WARN("""
        **자주 나오는 함정**: 제곱근을 구하려고 `x ** 1/2` 라고 쓰면 안 됩니다.
        `**` 가 `/` 보다 우선이라 `(x ** 1) / 2` 로 계산됩니다.
        제곱근은 `x ** (1/2)` 처럼 괄호를 씌워야 합니다.
        """),
        CODE("""
        x = 36

        print("x ** 1/2   =", x ** 1/2)
        print("x ** (1/2) =", x ** (1/2))
        """, note="위는 36을 2로 나눈 18, 아래는 진짜 제곱근 6.0 입니다."),
        CODE("""
        string = "문자열"
        number = 273
        print(string + number)
        """, note="서로 다른 자료를 연산하면 TypeError 입니다."),
    ], summary="int/float · ** 가 / 보다 우선 · 제곱근은 ** (1/2)"),
]

PROBLEMS = [
    problem("w3-p1", "변수로 정리하기", level=1, tags=["변수"],
        prompt=[
            P("반지름이 `10`인 원의 넓이를 출력하세요. 단, 원주율과 반지름을 각각 **변수에 저장**한 뒤 사용하세요."),
            OUT("원의 넓이 = 314.159265"),
            P("원주율은 `3.14159265`를 쓰세요."),
        ],
        tests=[test()],
        hints=[
            "먼저 pi = 3.14159265 처럼 값에 이름을 붙입니다.",
            "반지름도 r = 10 으로 저장하세요.",
            'print("원의 넓이 =", pi * r * r) 로 출력합니다.',
        ],
        solution='pi = 3.14159265\nr = 10\nprint("원의 넓이 =", pi * r * r)',
        explain=[
            P("변수를 쓰면 반지름을 바꾸고 싶을 때 `r = 10` 한 줄만 고치면 됩니다."),
            P("계산식 안에서 변수는 저장된 값으로 그대로 대체됩니다."),
            TIP("`pi * r ** 2` 로 써도 같은 값이 나옵니다. `**` 가 `*` 보다 먼저 계산되기 때문입니다."),
        ]),

    problem("w3-p2", "복합 대입 연산자", level=1, tags=["변수"], ptype="predict",
        prompt=[P("아래 코드의 실행 결과를 예측하세요.")],
        code="""
        a = 2
        a += 3
        a *= 4
        print(a)

        s = "안녕"
        s += "하세요"
        s *= 2
        print(s)
        """,
        hints=[
            "a += 3 은 a = a + 3 과 같습니다. 2 + 3 = 5.",
            "그 다음 a *= 4 는 a = a * 4 이므로 5 * 4 입니다.",
            "문자열에서 += 는 연결, *= 는 반복입니다.",
        ],
        explain=[
            P("`a`는 `2 → 5 → 20`으로 바뀝니다. 복합 대입은 **현재 값을 읽어서 계산한 뒤 다시 넣는** 동작입니다."),
            P("문자열 `s`는 `\"안녕\" → \"안녕하세요\" → \"안녕하세요안녕하세요\"`가 됩니다."),
        ]),

    problem("w3-p3", "이름 물어보기", level=1, tags=["input"],
        prompt=[
            P("사용자에게 이름을 입력받아 인사하는 프로그램을 만드세요."),
            P("프롬프트는 `이름이 뭔가요? ` 로, 출력은 아래 형식으로 하세요."),
            OUT("안녕하세요 김정미 님!"),
            P("오른쪽 입력값 칸의 값이 자동으로 입력됩니다."),
        ],
        tests=[test(stdin=["김정미"]), test(stdin=["박기동"])],
        hints=[
            'name = input("이름이 뭔가요? ") 로 입력을 받아 변수에 저장합니다.',
            "print() 에 쉼표로 넘기면 공백이 자동으로 들어가 형식이 안 맞을 수 있습니다.",
            'print("안녕하세요 " + name + " 님!") 처럼 + 로 이으면 공백을 직접 조절할 수 있습니다.',
        ],
        solution='name = input("이름이 뭔가요? ")\nprint("안녕하세요 " + name + " 님!")',
        explain=[
            P("`input()`이 돌려준 값을 변수에 저장해 두면 여러 번 쓸 수 있습니다."),
            TIP("쉼표는 공백을 자동으로 넣고 `+`는 넣지 않습니다. 출력 형식을 정확히 맞춰야 할 땐 `+`가 편합니다."),
        ]),

    problem("w3-p4", "두 수의 합", level=2, tags=["input", "형변환"],
        prompt=[
            P("숫자 두 개를 입력받아 **합**을 출력하세요. 프롬프트는 각각 `첫 번째 숫자> `, `두 번째 숫자> ` 입니다."),
            OUT("합: 325"),
            P("입력값이 `273`, `52`일 때의 결과입니다."),
        ],
        tests=[test(stdin=["273", "52"]), test(stdin=["100", "-30"])],
        hints=[
            "input()의 결과는 문자열이라 그냥 더하면 이어 붙습니다(27352).",
            "int() 로 감싸서 정수로 바꿔야 합니다.",
            'a = int(input("첫 번째 숫자> ")) 처럼 한 줄로 쓸 수 있습니다.',
        ],
        solution='a = int(input("첫 번째 숫자> "))\nb = int(input("두 번째 숫자> "))\nprint("합:", a + b)',
        explain=[
            P("`int(input(...))`은 괄호 **안쪽부터** 실행됩니다. `input()`이 문자열을 만들고, `int()`가 그걸 정수로 바꿉니다."),
            WARN("입력이 `52.273`처럼 소수면 `int()`는 ValueError를 냅니다. 그럴 땐 `float()`를 쓰세요."),
        ]),

    problem("w3-p5", "오류 찾기 — 자료형", level=2, tags=["형변환", "오류"], ptype="choice",
        prompt=[P("아래 코드 중 **오류가 발생하는 것**을 고르세요.")],
        options=[
            {"text": "①", "code": 'print(int("273") + 52)'},
            {"text": "②", "code": 'print(float("52.273") + 1)'},
            {"text": "③", "code": 'print(int("52.273") + 1)'},
            {"text": "④", "code": 'print(str(52) + "273")'},
        ],
        answer=2,
        hints=[
            "int() 는 소수점이 없는 숫자 형식의 문자열만 정수로 바꿀 수 있습니다.",
            "float() 는 소수점이 있어도 괜찮습니다.",
            '문자열 "52.273" 에는 소수점이 있습니다. int() 로 바꿀 수 있을까요?',
        ],
        explain=[
            P("③이 `ValueError: invalid literal for int() with base 10: '52.273'` 를 냅니다."),
            P("`int()`는 소수점이 있는 문자열을 받지 못합니다. `int(float(\"52.273\"))` 처럼 두 단계로 바꾸거나 `float()`를 쓰세요."),
            P("④는 `str(52)`가 `\"52\"`가 되어 문자열끼리 연결되므로 `52273`이 출력됩니다. 오류가 아닙니다."),
        ]),

    problem("w3-p6", "간단한 자기소개", level=3, tags=["input", "형변환"],
        prompt=[
            P("이름과 나이를 입력받아 **내년 나이**까지 알려 주는 프로그램을 만드세요."),
            P("프롬프트는 `이름> `, `나이> ` 이고 출력은 아래 형식입니다."),
            OUT("김정미 님은 올해 25살, 내년에는 26살입니다."),
            P("입력값이 `김정미`, `25`일 때의 결과입니다."),
        ],
        tests=[test(stdin=["김정미", "25"]), test(stdin=["이상우", "40"])],
        hints=[
            "나이는 계산에 쓰이므로 int() 로 바꿔야 합니다.",
            "출력할 때는 숫자를 다시 문자열과 이어 붙여야 합니다. 쉼표를 쓰면 공백이 들어가 형식이 안 맞습니다.",
            'str() 로 숫자를 문자열로 바꿔 + 로 이으세요.',
        ],
        solution='name = input("이름> ")\nage = int(input("나이> "))\nprint(name + " 님은 올해 " + str(age) + "살, 내년에는 " + str(age + 1) + "살입니다.")',
        explain=[
            P("`int()`로 바꿔야 `age + 1` 계산이 됩니다. 문자열 상태면 `TypeError`입니다."),
            P("다시 출력할 때는 `str()`로 되돌려야 `+`로 문자열과 이을 수 있습니다."),
            TIP("이 왔다 갔다 하는 변환이 번거롭죠. 실무에서는 f-문자열을 씁니다: `f\"{name} 님은 올해 {age}살\"`. 교재 범위 밖이지만 알아두면 편합니다."),
        ]),

    problem("w3-p7", "인덱싱 연습", level=1, tags=["인덱싱"],
        prompt=[
            P("문자열 `\"파이썬 프로그래밍\"` 에서 **첫 글자**와 **마지막 글자**를 각각 한 줄씩 출력하세요."),
            OUT("파\n밍"),
        ],
        tests=[test()],
        hints=[
            "첫 글자의 인덱스는 0 입니다.",
            "마지막 글자는 음수 인덱스 -1 로 간단히 고를 수 있습니다.",
            'print("파이썬 프로그래밍"[0]) 과 print("파이썬 프로그래밍"[-1]) 입니다.',
        ],
        solution='print("파이썬 프로그래밍"[0])\nprint("파이썬 프로그래밍"[-1])',
        explain=[
            P("제로 인덱스이므로 첫 글자는 `[0]`입니다."),
            P("길이를 몰라도 `[-1]`이면 항상 마지막 글자입니다. 이게 음수 인덱스의 장점입니다."),
        ]),

    problem("w3-p8", "슬라이싱 예측", level=2, tags=["슬라이싱"], ptype="predict",
        prompt=[P("아래 코드의 실행 결과를 예측하세요. 문자열은 `\"안녕하세요\"` 입니다.")],
        code="""
        s = "안녕하세요"
        print(s[1:3])
        print(s[:2])
        print(s[3:])
        print(len(s))
        """,
        hints=[
            "슬라이싱은 시작은 포함, 끝은 포함하지 않습니다.",
            "s[1:3] 은 1번과 2번 글자입니다. 3번은 빠집니다.",
            "인덱스는 안=0, 녕=1, 하=2, 세=3, 요=4 입니다.",
        ],
        explain=[
            P("`s[1:3]`은 1번(녕)과 2번(하)이라 `녕하`입니다."),
            P("`s[:2]`는 0번부터 2번 앞까지라 `안녕`입니다."),
            P("`s[3:]`은 3번부터 끝까지라 `세요`입니다."),
            TIP("끝 인덱스에서 시작 인덱스를 빼면 잘라낸 길이가 됩니다. `s[1:3]`은 `3-1=2`글자입니다."),
        ]),

    problem("w3-p9", "가운데 글자 뽑기", level=2, tags=["슬라이싱", "len"],
        prompt=[
            P("문자열 `\"프로그래밍\"` 에서 **가운데 세 글자**를 출력하세요."),
            OUT("로그래"),
        ],
        tests=[test()],
        hints=[
            "전체는 5글자이고, 앞뒤로 한 글자씩 빼면 됩니다.",
            "슬라이싱 [시작:끝] 을 쓰세요. 시작은 1번입니다.",
            "끝은 포함되지 않으므로 [1:4] 입니다.",
        ],
        solution='print("프로그래밍"[1:4])',
        explain=[
            P("인덱스는 프=0, 로=1, 그=2, 래=3, 밍=4 입니다."),
            P("`[1:4]`는 1, 2, 3번이라 `로그래`가 됩니다. 4번(밍)은 끝 미포함 규칙으로 빠집니다."),
            TIP("`[1:-1]` 로도 같은 결과가 나옵니다. 길이가 달라져도 앞뒤 한 글자씩 빼는 뜻이라 더 유연합니다."),
        ]),

    problem("w3-p10", "오류 찾기 — 문자열", level=3, tags=["문자열 연산자", "오류"], ptype="choice",
        prompt=[P("아래 코드 중 **오류가 발생하는 것**을 고르세요.")],
        options=[
            {"text": "①", "code": 'print("안녕하세요" * 3)'},
            {"text": "②", "code": 'print("안녕하세요"[2:100])'},
            {"text": "③", "code": 'print("안녕하세요"[10])'},
            {"text": "④", "code": 'print("안녕" + "하세요")'},
        ],
        answer=2,
        hints=[
            "인덱싱과 슬라이싱은 범위를 넘어갔을 때 동작이 다릅니다.",
            "슬라이싱은 범위를 넘어가도 오류가 나지 않고, 있는 만큼만 잘라 줍니다.",
            '"안녕하세요"는 5글자입니다. [10]은 존재하지 않는 위치를 하나 콕 집어 요구합니다.',
        ],
        explain=[
            P("③이 `IndexError: string index out of range` 를 냅니다."),
            P("②는 슬라이싱이라 오류가 나지 않고 `하세요`를 출력합니다. 인덱싱과 슬라이싱의 결정적 차이입니다."),
            TIP("이 차이는 리스트에서도 똑같이 적용됩니다. 6주차에서 다시 만납니다."),
        ]),

    problem("w3-p11", "제곱근의 함정", level=3, tags=["연산자", "우선순위"], ptype="predict",
        prompt=[
            P("아래 코드의 실행 결과를 예측하세요."),
            P("수학에서 제곱근은 `√x = x^(1/2)` 입니다."),
        ],
        code="""
        x = 36
        print(x ** 1/2)
        print(x ** (1/2))
        """,
        hints=[
            "** 는 / 보다 우선순위가 높습니다.",
            "첫 줄은 (36 ** 1) / 2 로 계산됩니다. 제곱근이 아닙니다.",
            "둘째 줄은 괄호 덕분에 36 ** 0.5 가 되어 진짜 제곱근이 나옵니다. 결과는 실수라 6.0 입니다.",
        ],
        explain=[
            P("첫 줄은 `36 ** 1 = 36`을 먼저 계산한 뒤 `2`로 나눠 `18.0`이 됩니다."),
            P("둘째 줄은 괄호 안 `1/2 = 0.5`가 먼저라 `36 ** 0.5 = 6.0`이 됩니다."),
            WARN("""
            수업 실습의 헤론의 공식에서 이 실수가 실제로 나왔습니다.
            `(s*(s-a)*(s-b)*(s-c))**1/2` 라고 쓰면 제곱근이 아니라 그냥 반으로 나누기가 됩니다.
            """),
            TIP("헷갈리면 `x ** 0.5` 라고 쓰는 방법도 있습니다."),
        ]),

    problem("w3-p12", "삼각형의 넓이", level=3, tags=["input", "형변환"],
        prompt=[
            P("밑변과 높이를 입력받아 삼각형의 넓이를 출력하세요."),
            P("프롬프트는 `밑변> `, `높이> ` 이고 출력은 아래 형식입니다."),
            OUT("넓이 = 10000.0"),
            P("입력값이 `100`, `200`일 때의 결과입니다. 넓이는 `밑변 × 높이 ÷ 2` 입니다."),
        ],
        tests=[test(stdin=["100", "200"]), test(stdin=["7", "4"])],
        hints=[
            "밑변과 높이 둘 다 int() 로 바꿔야 합니다. 한쪽만 바꾸면 오류가 납니다.",
            'b = int(input("밑변> ")) 처럼 양쪽 모두 감싸세요.',
            'print("넓이 =", b * h / 2) 입니다. / 로 나누면 결과가 실수라 .0 이 붙습니다.',
        ],
        solution='b = int(input("밑변> "))\nh = int(input("높이> "))\nprint("넓이 =", b * h / 2)',
        explain=[
            P("`/` 로 나누면 결과가 항상 실수라 `10000.0` 처럼 `.0` 이 붙습니다."),
            WARN("""
            수업 실습에서 한쪽만 `int()` 를 씌워 오류가 났습니다.
            둘 다 문자열이면 `"100" * "200"` 이 되어 `can't multiply sequence by non-int of type 'str'`,
            한쪽만 숫자면 문자열 반복이 된 뒤 `/ 2` 에서 오류가 납니다.
            """),
            TIP("`//` 를 쓰면 `10000` 처럼 정수로 나옵니다. 문제의 형식에 맞게 골라 쓰세요."),
        ]),
]
