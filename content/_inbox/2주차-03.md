# 2주차 · 배치 3 (마지막, 2장)

---

## 슬라이드 K — 5.2 자료 정리 · 비교 연산자 (표 6.3)

주차 마무리의 정리 슬라이드. 표 2.3 과 같은 내용을 다시 보여 준다.

| 연산자 | 의미 | 연산자 | 의미 |
|---|---|---|---|
| == | ~와 같으면 | != | ~와 같지 않다면 |
| > | ~보다 크면 | < | ~보다 작으면 |
| >= | ~보다 크거나 같으면 | <= | ~보다 작거나 같으면 |

---

## 슬라이드 L — 파이썬 공식 문서의 내장 함수 목록

교수님이 파이썬 공식 문서(docs.python.org)의 내장 함수 페이지를 띄워 보여 줌.

> "파이썬 인터프리터에는 항상 사용할 수 있는 많은 함수와 형이 내장되어 있습니다.
> 여기에서 알파벳 순으로 나열합니다."

화면에 보인 내장 함수들 (알파벳 순)

```
abs()          delattr()     hash()         memoryview()   set()
all()          dict()        help()         min()          setattr()
any()          dir()         hex()          next()         slice()
ascii()        divmod()      id()           object()       sorted()
bin()          enumerate()   input()        oct()          staticmethod()
bool()         eval()        int()          open()         str()
breakpoint()   exec()        isinstance()   ord()          sum()
bytearray()    filter()      issubclass()   pow()          super()
bytes()        float()       iter()         print()        tuple()
callable()     format()      len()          property()     type()
chr()          frozenset()   list()         range()        vars()
classmethod()  getattr()     locals()       repr()         zip()
compile()      globals()     map()          reversed()     __import__()
complex()      hasattr()     max()          round()
```

메모
- 지금까지 배운 `print, input, int, float, str, len, type, range` 가 모두
  이 목록 안에 있다는 것을 짚어 주면 좋다.
- "내장 함수는 import 없이 바로 쓸 수 있고, random 같은 모듈 함수는
  import 가 필요하다" 는 대비가 자연스럽게 나온다.
- 공식 문서 주소: https://docs.python.org/ko/3/library/functions.html

---

## 적용 시 만들 예제 후보 (배치 3)

- 내장 함수와 모듈 함수 구분하기 (`len()` 은 그냥 쓰고 `random.randint()` 는 import 필요)
- 비교 연산자 결과 예측 (True/False)
- 지금까지 쓴 함수가 내장 함수 목록의 어디에 있는지 찾아보기
